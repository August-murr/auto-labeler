name: "OpenAI Issue Labeler"
on:
  issues:
    types: opened

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: August-murr/auto-labeler@v0.0.1
        with:
            openai-api-key: ${{ secrets.OPENAI_API_KEY }}
